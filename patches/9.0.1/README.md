# FFmpeg 9.0.1 Patches

The initial Larix packaging revision applies no source patches. Any future
`*.patch` file in this directory is ordered lexicographically, hashed into
the preparation state, checked with `git apply --check`, and then applied.
